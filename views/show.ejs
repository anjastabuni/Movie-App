<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detail Film - <%= movie.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white">
    <div class="container mx-auto p-6">
      <div class="flex flex-col md:flex-row items-center">
        <img src="https://image.tmdb.org/t/p/w500<%= movie.poster_path %>" class="w-full md:w-1/3 rounded-lg shadow-lg" />
        <div class="ml-6">
          <h1 class="text-3xl font-bold"><%= movie.title %></h1>
          <p class="text-gray-400 mt-2">⭐ <%= movie.vote_average %> | 🗓 <%= movie.release_date %></p>
          <p class="mt-4"><%= movie.overview %></p>
          <a href="/" class="mt-4 inline-block bg-blue-600 px-4 py-2 rounded text-white">Kembali</a>
        </div>
      </div>
    </div>
  </body>
</html>
